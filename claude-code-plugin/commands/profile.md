---
description: Show today's Claude Rank profile card with activity timeline and concurrency stats
---
Run the commands below and display the output exactly as-is. Do not reformat, summarize, or add commentary.

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/sync.mjs" 2>/dev/null; node "${CLAUDE_PLUGIN_ROOT}/scripts/fetch-profile.mjs"
```
